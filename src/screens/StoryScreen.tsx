import React from 'react';
import {View, Text} from 'react-native'

interface Props { }
const StoryScreen: React.FC<Props> = (props) => {
    return (
        <View>
            <Text>This is StoryScreen</Text>
        </View>
    );
}

export default StoryScreen;